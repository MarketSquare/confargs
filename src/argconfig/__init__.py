"""argconfig: declarative CLI parsing that merges CLI, env vars and TOML config.

The full public API is assembled across later modules. For now this exposes the
package version; option/base/processor symbols are added incrementally.
"""

from __future__ import annotations

__version__ = "0.1.0"

__all__ = ["__version__"]
