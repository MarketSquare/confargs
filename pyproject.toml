[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "confargs"
dynamic = ["version"]
description = "Declarative CLI argument parser that merges command line, TOML config files, and environment variables."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "confargs contributors" }]
keywords = ["cli", "argparse", "toml", "configuration", "arguments"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
dependencies = [
    "tomli>=2.0; python_version < '3.11'",
]

[project.urls]
Homepage = "https://github.com/MarketSquare/confargs"
Repository = "https://github.com/MarketSquare/confargs"
Issues = "https://github.com/MarketSquare/confargs/issues"

[project.scripts]
confargs-demo = "confargs.demo:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "mypy>=2.3.1",
    "ruff>=0.16.5",
    "pre-commit>=3.7",
    "tomli>=2.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/confargs"]

[tool.hatch.version]
path = "src/confargs/__init__.py"

[tool.ruff]
line-length = 120
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "C4", "PTH", "RUF"]
ignore = []

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011"]

[tool.mypy]
python_version = "3.10"
strict = true
files = ["src/confargs"]
warn_unused_ignores = true
warn_redundant_casts = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.coverage.run]
source = ["confargs"]
branch = true
